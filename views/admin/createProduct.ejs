<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register - Crown Source</title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Alegreya+Sans+SC:ital,wght@0,100;0,300;0,400;0,500;0,700;0,800;0,900;1,100;1,300;1,400;1,500;1,700;1,800;1,900&display=swap");

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
      }

      body {
        background-color: #121212;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .register-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100%;
        width: 100%;
      }

      .register-box {
        background-color: #1c1c1c;
        padding: 15px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        text-align: center;
        width: 92vw;
        color: #d4af37;
      }

      .logo img {
        width: 120px;
        height: 170px;
        object-fit: cover;
      }

      h1 {
        font-size: 24px;
        font-weight: 400;
        margin-top: 15px;
        line-height: 1;
        text-transform: uppercase;
        font-family: "Alegreya Sans SC", sans-serif;
      }

      p {
        font-size: 22px;
      }

      form {
        display: flex;
        flex-direction: column;
        margin: 20px auto 35px auto;
      }

      input[type="text"],
      input[type="number"] {
        background-color: transparent;
        border: none;
        outline: none;
        border-bottom: 1px solid #d4af37;
        padding: 10px;
        margin-bottom: 10px;
        color: #fff;
        font-size: 20px;
        width: 100%;
      }

      textarea#description {
        background-color: transparent;
        border: none;
        outline: none;
        border-bottom: 1px solid #d4af37;
        padding: 10px;
        margin-bottom: 10px;
        color: #fff;
        font-size: 20px;
        resize: none;
        width: 100%;
      }

      button {
        margin-top: 20px;
        background-color: #d4af37;
        border: none;
        border-radius: 5px;
        padding: 10px 20px;
        color: #1c1c1c;
        font-weight: bold;
        cursor: pointer;
        font-size: 18px;
        text-transform: uppercase;
      }

      a {
        display: block;
        margin-top: 10px;
        color: #d4af37;
        text-decoration: none;
        font-size: 18px;
      }

      input[type="file"] {
        font-size: 20px;
        border-radius: 10px;
        margin-top: 10px;
        padding: 0 15px;
        cursor: pointer;
      }

      .brand {
        display: flex;
        align-items: center;
        justify-content: center;
        margin-top: 20px;
        gap: 15px;
        font-size: 17px;
        text-transform: uppercase;
      }

      .brand select {
        font-size: 15px;
        padding: 5px 5px;
        background: transparent;
        color: white;
        border-radius: 5px;
      }

      .brand select option {
        background: #1c1c1c;
        font-size: 20px;
      }

      .stock {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 15px;
        font-size: 20px;
      }

      .stock input {
        width: 20px;
        height: 20px;
      }

      /* Chrome, Safari, Edge, Opera */
      input[type="number"]::-webkit-outer-spin-button,
      input[type="number"]::-webkit-inner-spin-button {
        -webkit-appearance: none;
      }
    </style>
  </head>
  <body>
    <div class="register-container">
      <div class="register-box">
        <div class="logo">
          <img src="/images/logo.png" alt="Crown Source Logo" />
        </div>
        <h1>Create New Product</h1>
        <form
          action="/admin/product/add"
          method="post"
          enctype="multipart/form-data"
        >
          <input
            type="text"
            name="name"
            placeholder="Enter Product Name"
            required
          />
          <textarea
            name="description"
            placeholder="Enter Description"
            id="description"
            rows="2"
            required
          ></textarea>
          <input type="file" accept="image/*" name="image" />
          <div class="brand">
            <label for="brand">Brand :</label>
            <select name="brand" id="category" required>
              <option value="">Select a Brand</option>
              <option value="Mix">MIX</option>
              <option value="Acp">ACP</option>
            </select>
          </div>
          <input
            type="text"
            name="category"
            placeholder="Enter Category e.g : headphone"
            required
          />
          <div class="stock">
            <label for="inStock">In Stock :</label>
            <input type="checkbox" name="inStock" id="inStock" checked />
          </div>
          <button type="submit">Add Product</button>
        </form>
      </div>
    </div>
  </body>
</html>
